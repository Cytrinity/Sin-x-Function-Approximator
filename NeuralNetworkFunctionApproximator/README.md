# Sin-x-Function-Approximator
A neural network designed from scratch in .NET to approximate the sin(x) function or other functions given to it. Uses a single-layer feedforward neural network and tests/collects data on varying numbers of inner neurons.
